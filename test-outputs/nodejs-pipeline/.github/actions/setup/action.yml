name: Setup Action
description: Enhanced composite action for Setup stage with comprehensive Jenkins feature support
inputs: {}
runs:
  using: composite
  steps:
  - name: Run command 1
    run: node --version
    shell: bash
  - name: Run command 2
    run: npm --version
    shell: bash
  - name: Run command 3
    run: npm ci --prefer-offline --no-audit
    shell: bash
