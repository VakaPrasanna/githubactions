name: Build Package Action
description: Enhanced composite action for Build Package stage with comprehensive Jenkins feature support
inputs: {}
runs:
  using: composite
  steps:
  - name: Run command 1
    run: python setup.py sdist bdist_wheel
    shell: bash
  - name: Run command 2
    run: pip wheel . -w dist/
    shell: bash
