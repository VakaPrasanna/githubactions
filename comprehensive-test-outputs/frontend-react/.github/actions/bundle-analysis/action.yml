name: Bundle Analysis Action
description: Enhanced composite action for Bundle Analysis stage with comprehensive Jenkins feature support
inputs: {}
runs:
  using: composite
  steps:
  - name: Run command 1
    run: npm run analyze -- --no-open --report-filename=bundle-report.html
    shell: bash
  - name: Run command 2
    run: npx bundlesize
    shell: bash
  - name: publishHTML (REQUIRES MANUAL CONVERSION)
    run: "\n# MANUAL CONVERSION REQUIRED: publishHTML\n# Original Jenkins feature: publishHTML([\n                        allowMissing: true,\n                        alwaysLinkToLastBu\n# Action needed: Review Jenkins documentation and implement equivalent logic\n# TODO: Implement this functionality manually\n"
    shell: bash
