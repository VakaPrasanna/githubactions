name: Validate Kubernetes Config Action
description: Enhanced composite action for Validate Kubernetes Config stage with comprehensive Jenkins feature support
inputs: {}
runs:
  using: composite
  steps:
  - name: Run command 1
    run: kubectl cluster-info
    shell: bash
  - name: Run command 2
    run: helm version
    shell: bash
  - name: Run Kubernetes command 1
    run: kubectl cluster-info
    shell: bash
  - name: Run Kubernetes command 2
    run: helm version
    shell: bash
